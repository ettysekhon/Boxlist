const config = {
  spacing: {
    baseFontSize: 16
  }
};
const baseFontSize = config.spacing.baseFontSize;

const fonts = {
  size: {
    xxxs: baseFontSize * 0.5,
    xxs: baseFontSize * 0.625,
    xs: baseFontSize * 0.75,
    s: baseFontSize * 0.875,
    m: baseFontSize,
    l: baseFontSize * 1.25,
    xl: baseFontSize * 1.5,
    xxl: baseFontSize * 2,
    xxxl: baseFontSize * 4
  }
};

export default fonts;
